# CUDA 12.1 PyTorch wheels (GPU baseline).
# The `+cu121` suffix exists only on PyTorch's CUDA index, which forces pip
# to fetch the GPU wheel instead of the default CPU build from PyPI.
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.5.1+cu121

# PDF extraction
PyMuPDF>=1.24.0
pdfplumber>=0.11.0

# Chunking
syntok>=1.4.4
tiktoken>=0.7.0

# Retrieval
rank-bm25>=0.2.2
chromadb>=0.5.0
sentence-transformers>=2.2.0

# Generation
ollama>=0.4.0

# Frontend
fastapi>=0.115.0
uvicorn>=0.34.0
jinja2>=3.1.0

# Evaluation
ragas>=0.4.0
openai>=1.0.0
python-dotenv>=1.0.0

# Testing
pytest>=8.0.0
pytest-cov>=6.0.0
